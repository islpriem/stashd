# The LOC2HOT storage daemon inside the e2e container, the receiving side of an ssh warm.
self:
  daemon_id: loc2hot
  role: storage
  storages: [LOC2HOT]
controller: { url: "http://127.0.0.1:8000", token_file: peer-token }
server: { host: 127.0.0.1, port: 8002 }
logging: { level: INFO, format: json }
cache_dir: var/loc2hot
peer_token_file: peer-token
# Become the user with sudo -n -u.
identity: sudo
worker_pool_size: 4
